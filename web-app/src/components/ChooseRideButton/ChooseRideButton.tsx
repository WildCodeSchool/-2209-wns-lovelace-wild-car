import { Center, SButton } from './ChooseRideButton.styled';

const ChooseRideButton = () => {
  return (
    <Center>
      <SButton>Choisir ce trajet</SButton>
    </Center>
  );
};

export default ChooseRideButton;
